# The frontend talks to the backend through Vite's dev proxy (see vite.config.js),
# so no variable is needed for local development.
# Set this only when the API is deployed somewhere other than the same origin.
# VITE_API_BASE_URL=https://tracex-api.example.gov
